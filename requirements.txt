# Core API and processing
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.6
opencv-python-headless>=4.9
yt-dlp>=2024.0

# Metadata store (MongoDB Atlas)
pymongo>=4.6
dnspython>=2.4
certifi>=2023.0

# Object storage client (S3-compatible: Cloudflare R2, B2, S3, MinIO)
minio>=7.2

# Configuration
python-dotenv>=1.0

# Auto-annotation OCR. Optional: the /api/dataset/images/{id}/ocr endpoint
# reports that OCR is unavailable if this is not installed.
rapidocr-onnxruntime>=1.3
